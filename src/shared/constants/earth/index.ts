// Orbit Control Min Distance
export const CONTROL_MIN_ZOOM = 1;
// Orbit Control Max Distance
export const CONTROL_MAX_ZOOM = 3;
// Orbit Control Min Polar Angle
export const CONTROL_MIN_POLAR_ANGLE = Math.PI / 2 - 0.78;
// Orbit Control Max Polar Angle
export const CONTROL_MAX_POLAR_ANGLE = Math.PI / 2 + 0.78;

// switch between earth globe and mapbox
export const MAPBOX_SWITCH_THRESHOLD = 3;

// mapbox decide to show the markers in bundle mode or single mode
export const MAPBOX_MARKER_BUNDLE_THRESHOLD = 6;
